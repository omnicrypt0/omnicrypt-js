{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node scripts/build",
    "test": "npm run build && node scripts/test"
  },
  "author": "Carter Snook",
  "license": "MIT",
  "dependencies": {
    "glob": "^7.1.6"
  }
}
